import { CartFooterPropTypes, CartFooterProps } from './types'

const CartFooter = ({ total }: CartFooterProps) => {
  return <div></div>
}

CartFooter.propTypes = CartFooterPropTypes
export default CartFooter
