import { CarHeaderPropTypes, CarHeaderProps } from './types'

const CartHeader = ({ closeCart, emptyCart }: CarHeaderProps) => {
  return <div>CartContentHeader</div>
}

CartHeader.propTypes = CarHeaderPropTypes

export default CartHeader
